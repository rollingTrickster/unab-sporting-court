# ========================================
# CONFIGURACIÓN DE VARIABLES DE ENTORNO
# ========================================
# Copia este archivo como .env y ajusta los valores según tu entorno

# ========================================
# BASE DE DATOS
# ========================================
# PostgreSQL (Producción/Docker)
POSTGRES_USER=sporting_user
POSTGRES_PASSWORD=sporting_password
POSTGRES_DB=sporting_court_db

# URL de conexión a la base de datos
# Para PostgreSQL (Docker):
# DATABASE_URL=postgresql://sporting_user:sporting_password@db:5432/sporting_court_db
# Para PostgreSQL (Local):
# DATABASE_URL=postgresql://sporting_user:sporting_password@localhost:5432/sporting_court_db
# Para SQLite (Desarrollo):
DATABASE_URL=sqlite:///./sporting_court.db

# ========================================
# SEGURIDAD JWT
# ========================================
# Clave secreta para firmar tokens JWT (CAMBIAR EN PRODUCCIÓN)
# Genera una clave segura con: openssl rand -hex 32
SECRET_KEY=your-secret-key-change-in-production-use-openssl-rand-hex-32

# Algoritmo de encriptación
ALGORITHM=HS256

# Tiempo de expiración del token en minutos
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ========================================
# CORS (Orígenes permitidos)
# ========================================
# Lista de orígenes permitidos para CORS (formato JSON)
# Desarrollo:
CORS_ORIGINS=["http://localhost:8080","http://localhost:3000","http://127.0.0.1:8080","http://localhost:5173"]

# ========================================
# NOTAS DE SEGURIDAD
# ========================================
# 1. NUNCA subas el archivo .env al repositorio
# 2. Cambia SECRET_KEY en producción
# 3. Usa contraseñas fuertes para la base de datos
# 4. Limita CORS_ORIGINS a dominios específicos en producción

